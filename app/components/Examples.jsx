var React = require('react');

var Examples = (props) => {
    return (
        <div>
            <h3>Examples</h3>
            <p>Welcome to the examples component!!</p>
        </div>

    );
  };

module.exports = Examples;
